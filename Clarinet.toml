[project]
name = 'Food-Safety-Recall-Registry'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.lot-registry]
path = 'contracts/lot-registry.clar'
clarity_version = 3
epoch = '3.2'

[contracts.recall-broadcast]
path = 'contracts/recall-broadcast.clar'
clarity_version = 3
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
